# 设计后处理案例索引

## 已验证主题

当前仓库尚未收录可公开的完整设计后处理案例。

## 优先补充

- `VirtualModel.newInstance` 追加虚拟模型；
- `ModelPostProcessResult.addChildren`；
- 添加/更新模型参数；
- 顶层模型与子模型遍历；
- `service.dcs-post-process-task` 日志诊断；
- 设计后处理与台面工艺费链路的边界。

提交案例时使用仓库成功案例模板，并附脱敏输出 JSON 验证。
