# Codex Communication

- 默认使用简体中文。
- 将成功经验按业务领域写入 `skills-共享技能/`，不要按作者拆 Skill。
- 新案例必须基于真实脚本、日志或输出证据。
- 公开内容必须遵循 `SECURITY-安全与脱敏规范.md`。
- Skill 目录和 frontmatter `name` 使用小写英文连字符。
- 除机器固定文件名外，文档文件名使用 `english-slug-中文说明`。
- 修改 Skill 后运行 `python scripts-工具脚本/validate-skills-校验技能.py`。
